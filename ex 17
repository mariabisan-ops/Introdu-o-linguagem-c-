#include <stdio.h>

int main (){
    float pi, raio, area;
    pi = 3.14;
    printf ("digite o valor do raio:\n");
    scanf ("%f", &raio);
    area = pi*(raio*raio);
    printf ("o valor da area é: %.2f", area);
    
}
